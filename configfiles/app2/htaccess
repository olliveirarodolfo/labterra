<IfModule mod_rewrite.c>
RewriteEngine On
RewriteCond %{REQUEST_URI} ^/app2 [OR]
RewriteCond %{REQUEST_URI} ^/probe
RewriteCond %{REQUEST_URI} !=/favicon.ico
RewriteRule (.*) index.php
</IfModule>
